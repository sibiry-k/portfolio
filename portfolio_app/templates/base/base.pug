!!! 5
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="yandex-verification" content="274656319693c12e")
    meta(name="description" content="Привет! Я Антон — Python backend-разработчик. Создаю web-приложения на Django, FastAPI и Flask, работаю с PostgreSQL и Docker, настраиваю CI/CD и пишу качественный REST API. Открыт к предложениям удалённой работы и новым проектам.")
    meta(name="keywords" content="Python разработчик, backend разработка, Django, FastAPI, Flask, PostgreSQL, Docker, CI/CD, REST API, pytest, удалённая работа, веб-разработка, бэкенд")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Антон Иванов - Python Backend Developer | Python-разработчик
    link(rel="canonical" href="https://sibiryk.ru")
    link(rel='icon' type='image/png' href='{{url_for("static", filename="img/favicon/favicon.png") }}')
    link(rel='icon' type='image/png' sizes='32x32' href='{{url_for("static", filename="img/favicon/favicon-32x32.png") }}')
    link(rel='icon' type='image/png' sizes='192x192' href='{{url_for("static", filename="img/favicon/favicon-192x192.png") }}')
    link(rel='apple-touch-icon' type='image/png' href='{{url_for("static", filename="img/favicon/favicon-192x192.png") }}')
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap")
    link(rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}")
    script(src="{{ url_for('static', filename='js/htmx.min.js') }}")
    script(src="https://smartcaptcha.yandexcloud.net/captcha.js" async defer)
    // Yandex.Metrika counter
    script.
      (function(m,e,t,r,i,k,a){
        m[i]=m[i]||function(){
          (m[i].a=m[i].a||[]).push(arguments)
        };
        m[i].l=1*new Date();
        for (var j = 0; j < document.scripts.length; j++) {
          if (document.scripts[j].src === r) { return; }
        }
        k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)
      })
      (window, document,'script','https://mc.yandex.ru/metrika/tag.js', 'ym');
      ym(4847245, 'init', {
        clickmap:true,
        accurateTrackBounce:true,
        trackLinks:true
      });
  body
    // Yandex.Metrika counter
    noscript
      div
        img(
          src='https://mc.yandex.ru/watch/4847245'
          style='position:absolute; left:-9999px;'
          alt=''
        )
    block header
      include base/header
    main
      block content
    block footer
      include base/footer
    script.
      var navbar = document.getElementById('navbar');
      var burgerBtn = document.getElementById('burger-btn');
      var mobileMenu = document.getElementById('mobile-menu');

      window.addEventListener('scroll', function () {
        navbar.classList.toggle('scrolled', window.scrollY > 10);
      });

      burgerBtn.addEventListener('click', function () {
        mobileMenu.classList.toggle('open');
      });